import { BoxBounds } from './types';

export const MAP_BOXES: BoxBounds[] = [];

function add(x: number, z: number, w: number, h: number, d: number) {
    MAP_BOXES.push({ cx: x, cz: z, cy: h / 2, hw: w / 2, hd: d / 2, hh: h / 2 });
}

// ─── Boundary Walls ──────────────────────────────────────────────────────────
add(  0, -48, 98, 4, 1);   // North
add(  0,  48, 98, 4, 1);   // South
add( 48,   0,  1, 4, 98);  // East
add(-48,   0,  1, 4, 98);  // West

// ─── Center Structure ────────────────────────────────────────────────────────
add(0, 0, 10, 0.6, 10);    // low platform
add(0, 0,  3,   6,  3);    // tall tower

// ─── NW Compound ─────────────────────────────────────────────────────────────
add(-28, -22,    1, 3.5,  12);
add(-22, -28,   12, 3.5,   1);
add(-22, -22,    6, 2.5,   1);
add(-25, -25,    2,   2,   2);
add(-25, -19,  1.5, 1.2, 1.5);

// ─── NE Compound ─────────────────────────────────────────────────────────────
add( 28, -22,    1, 3.5,  12);
add( 22, -28,   12, 3.5,   1);
add( 22, -22,    6, 2.5,   1);
add( 25, -25,    2,   2,   2);
add( 25, -19,  1.5, 1.2, 1.5);

// ─── SW Compound ─────────────────────────────────────────────────────────────
add(-28,  22,    1, 3.5,  12);
add(-22,  28,   12, 3.5,   1);
add(-22,  22,    6, 2.5,   1);
add(-25,  25,    2,   2,   2);
add(-25,  19,  1.5, 1.2, 1.5);

// ─── SE Compound ─────────────────────────────────────────────────────────────
add( 28,  22,    1, 3.5,  12);
add( 22,  28,   12, 3.5,   1);
add( 22,  22,    6, 2.5,   1);
add( 25,  25,    2,   2,   2);
add( 25,  19,  1.5, 1.2, 1.5);

// ─── Mid Crossing Walls ─────────────────────────────────────────────────────
add(-7, -15, 8, 2.5, 1);
add( 7, -15, 8, 2.5, 1);
add( 0, -18, 2, 1.5, 2);
add(-7,  15, 8, 2.5, 1);
add( 7,  15, 8, 2.5, 1);
add( 0,  18, 2, 1.5, 2);

// ─── East Lane ───────────────────────────────────────────────────────────────
add( 38,   0, 3, 5, 3);
add( 38, -12, 4, 2, 1);
add( 38,  12, 4, 2, 1);
add( 40, -35, 5, 2.5, 3);
add( 40,  35, 5, 2.5, 3);

// ─── West Lane ───────────────────────────────────────────────────────────────
add(-38,   0, 3, 5, 3);
add(-38, -12, 4, 2, 1);
add(-38,  12, 4, 2, 1);
add(-40, -35, 5, 2.5, 3);
add(-40,  35, 5, 2.5, 3);

// ─── Inner Cover ─────────────────────────────────────────────────────────────
add(-12, -8, 1, 2.2, 3);
add( 12, -8, 1, 2.2, 3);
add(-12,  8, 1, 2.2, 3);
add( 12,  8, 1, 2.2, 3);

// ─── Small Crates ────────────────────────────────────────────────────────────
add(-5, -5, 1.5, 1, 1.5);
add( 5,  5, 1.5, 1, 1.5);
add(-5,  5, 1.5, 1, 1.5);
add( 5, -5, 1.5, 1, 1.5);

// ─── Mid Lane Barriers ──────────────────────────────────────────────────────
add(-20, 0, 3, 1.5, 1);
add( 20, 0, 3, 1.5, 1);

// ─── Outer Area Crates ──────────────────────────────────────────────────────
add(-10, -35, 3, 1.8, 2);
add( 10, -35, 3, 1.8, 2);
add(-10,  35, 3, 1.8, 2);
add( 10,  35, 3, 1.8, 2);

// ─── Approach Steps ─────────────────────────────────────────────────────────
add( 7,  0, 2, 0.3, 2);
add(-7,  0, 2, 0.3, 2);
add( 0,  7, 2, 0.3, 2);
add( 0, -7, 2, 0.3, 2);
